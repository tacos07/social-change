{
  "name": "signal-join-gate",
  "version": "1.0.0",
  "description": "Email-gated access to a private Signal group. Validates an institutional email via a one-time code, then time-limits a browser-bound redirect to the Signal invite link. Stores nothing.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "js-yaml": "^4.1.0",
    "nodemailer": "^9.0.3"
  }
}
